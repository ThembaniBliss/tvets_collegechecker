name: tvets_collegechecker
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase: ^2.3.0  # Upgrade to the latest compatible version
  image_picker: ^0.8.4+11
  flutter_dotenv: ^5.0.2
  postgrest: ^2.1.4  # Keep the required version
  provider: ^6.0.3
  supabase_flutter: ^2.6.0
  shared_preferences: ^2.0.7 


  cupertino_icons: ^1.0.6
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mockito: ^5.4.4

flutter:
  uses-material-design: true
  assets:
    - assets/.env
